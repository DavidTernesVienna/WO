{
  "name": "lean-interval-timer-pwa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r public/* dist/ && cp src/service-worker.js dist/service-worker.js",
    "start": "node server.js",
    "test": "node tests/run-tests.js"
  },
  "devDependencies": {
    "puppeteer": "^23.8.1"
  }
}
